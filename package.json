{
  "name": "fe_test_env",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "lodash": "^4.17.21"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "reset": "git fetch origin && git reset --hard origin/master && git clean -df"
  },
  "devDependencies": {
    "eslint": "^8.23.1",
    "eslint-plugin-import": "^2.26.0",
    "eslint-plugin-unused-imports": "^2.0.0"
  }
}
